---
title: "Sueño"
format: html
editor: visual
---

```{r}
rm(list = ls())
library(corrplot)
library(wooldridge)
library(FactoMineR)
library(dplyr, pos = "package:base")
sueno <- sleep75
```

```{r}
glimpse(sueno)
```

```{r}
corrplot(cor(sueno))
```

```{r}
princomp(sueno)
```



```{r}
PCA(sueno)
```

